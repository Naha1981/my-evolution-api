#!/bin/bash
set -e

echo "🚀 Starting Evolution API deployment on Render..."

# Run database migrations
echo "📦 Running database migrations..."
npx prisma generate
npx prisma migrate deploy

echo "✅ Migrations complete. Starting server..."
exec npm start
